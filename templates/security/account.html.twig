{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} :: Mon Compte{% endblock %}

{% block body %}
    <h1><i class="bi bi-person-circle"></i> Mon Compte</h1>
    <div class="well">
        <div class="row justify-content-md-center txt-center">
            <div class="col-12 col-lg-6">
                {% if is_granted('IS_AUTHENTICATED') %}
                    <p>Vous pouvez changer d'email et de mot de passe si vous le souhaitez, mais vous garderez tous vos autres soucis, ainsi va la vie.</p>
                {% else %}
                    <p>Il nous faut un email en bonne et dûe forme ainsi qu'un mot de passe solide dont il faudra se souvenir pour venir nous voir.</p>
                {% endif %}
                {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
                {{ form_errors(form) }}
                {{ form_widget(form) }}
                <div class="text-end">
                    <button type="submit" class="btn btn-primary"><i class="bi bi-check-circle-fill"></i> OK</button>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
