{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} :: Liens{% endblock %}

{% block body %}
    <div class="p-4">
        <h1><i class="bi bi-link"></i> Liens</h1>
        <div class="row row-cols-1 row-cols-md-2 row-cols-xl-3 g-2 justify-content-center txt-center">
            {% for categ in categories %}
                <div class="col p-2">
                    <div class="flux card">
                        <div class="card-header text-center">
                            <h2><i class="bi bi-layers-fill me-2"></i>{{ categ.name|capitalize }}</h2>
                        </div>
                        <div class="card-body">
                            <div class="list-group">
                                {% for link in liens %}
                                    {% if link.mood is not null and link.mood.id == categ.id %}
                                        <div class="d-flex list-group-item list-group-item-action list-group-item-info justify-content-between border-1">
                                            <a href="{{ link.url }}" class="" target="_blank"
                                               rel="noopener noreferrer">
                                                <h3>{{ link.name }}</h3>
                                                <p class="text-black-50">{{ link.description }}</p>
                                            </a>
                                            {% if is_granted('FLUX_EDIT', link) %}
                                                <div>
                                                    <a href="{{ path('flux_edit',{'id': link.id}) }}"
                                                       class="btn btn-outline-primary">
                                                        <i class="bi bi-pencil"></i>
                                                    </a>
                                                </div>
                                            {% endif %}
                                        </div>
                                    {% endif %}
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
